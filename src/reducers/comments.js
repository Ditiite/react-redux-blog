/*
* Reducer takes in two things:
* 1. the action (info about what happened)
* 2. copy of current state.
*/

// {{const comments = (state = [], action) => {
//     console.log('This is comments',state);
//     return state;
// }

// export default comments;}}

export default function(){
    return [
        {
            author: "me"
        }
    ];
}
